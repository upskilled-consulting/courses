{
  "id": "q4",
  "moduleId": "m4",
  "title": "Production Systems Quiz",
  "passingScore": 0.7,
  "allowRetry": true,
  "showAnswersAfterSubmit": true,
  "prevItem": {
    "id": "r4",
    "type": "reading",
    "title": "Observability"
  },
  "nextItem": {
    "id": "d4",
    "type": "drill",
    "title": "Skills, Security & Observability"
  },
  "questions": [
    {
      "id": "q4_1",
      "type": "multiple-choice",
      "prompt": "A skill is registered with hook='post_synthesis' and explicit_only=True. Under what conditions does it activate?",
      "options": [
        {
          "id": "a",
          "text": "It activates automatically when task complexity == 'high'",
          "correct": false
        },
        {
          "id": "b",
          "text": "It activates only when the user prefixes the task with /skillname",
          "correct": true
        },
        {
          "id": "c",
          "text": "It activates after every successful Wiggum pass",
          "correct": false
        },
        {
          "id": "d",
          "text": "It activates when the auto_trigger predicate returns True, regardless of the explicit_only flag",
          "correct": false
        }
      ],
      "explanation": "explicit_only=True means the auto_trigger predicate is never evaluated. The skill only activates when the user explicitly includes /skillname in the task string. The post_synthesis hook determines when the handler runs (after output is written), not when the skill activates.",
      "hint": "explicit_only overrides auto_trigger — it means the skill will never self-activate."
    },
    {
      "id": "q4_2",
      "type": "multiple-choice",
      "prompt": "The orchestrator is given a task: 'Research the complete history of reinforcement learning and write a comprehensive guide.' Subtask 1 covers 1950–1990, subtask 2 covers 1990–2010, subtask 3 covers 2010–present. Subtask 3's output repeatedly covers material from Mnih et al. 2013 (DQN), which subtask 2 also covered. What caused this?",
      "options": [
        {
          "id": "a",
          "text": "MAX_WORKERS=4 caused thread-safety issues in the shared search cache",
          "correct": false
        },
        {
          "id": "b",
          "text": "The subtask prompts used only positive scope boundaries ('cover 2010–present') without explicitly marking what subtask 2 covers as out-of-scope",
          "correct": true
        },
        {
          "id": "c",
          "text": "The Wiggum loop should have caught this before assembly",
          "correct": false
        },
        {
          "id": "d",
          "text": "The research cache served both subtasks the same results",
          "correct": false
        }
      ],
      "explanation": "Role ambiguity failure: DQN (2013) falls in the boundary zone between subtask 2 (1990–2010) and subtask 3 (2010–present). Without explicit out-of-scope boundaries, both subtasks include it. The fix is adding out_of_scope=['DQN, 2013, Mnih et al. covered in subtask 2'] to subtask 3's prompt.",
      "hint": "Which failure class from Module 3 describes this? What countermeasure does the orchestrator reading prescribe?"
    },
    {
      "id": "q4_3",
      "type": "multiple-choice",
      "prompt": "The code scanner blocks `import requests`. A model-generated script uses `urllib.request.urlopen()` instead. What happens?",
      "options": [
        {
          "id": "a",
          "text": "The scanner catches it — urllib is in BLOCKED_IMPORTS",
          "correct": true
        },
        {
          "id": "b",
          "text": "The scanner misses it — only 'requests' is blocked, not 'urllib'",
          "correct": false
        },
        {
          "id": "c",
          "text": "The scanner catches it — urlopen is in BLOCKED_BUILTINS",
          "correct": false
        },
        {
          "id": "d",
          "text": "The scanner runs the code in a sandbox first to detect network calls",
          "correct": false
        }
      ],
      "explanation": "urllib is in BLOCKED_IMPORTS: {\"os\", \"subprocess\", \"sys\", \"shutil\", \"socket\", \"requests\", \"urllib\", \"pathlib\", \"ftplib\", \"smtplib\"}. The scanner runs a raw-text pass first (for obfuscation detection) then an AST pass. The raw-text pass checks BLOCKED_ATTR_PATTERNS — import urllib.request is a straightforward literal import, not an obfuscated form, so it reaches the AST pass. There, the Import node is walked and the root module name 'urllib' is found in BLOCKED_IMPORTS. Caught.",
      "hint": "Check the full BLOCKED_IMPORTS set from the Security Layer reading."
    },
    {
      "id": "q4_4",
      "type": "multiple-choice",
      "prompt": "A run with the 32B producer makes 4 model calls: plan (glm4:9b), synthesis, wiggum_r1, wiggum_r2 (Qwen3-Coder:30b for both). Ollama keep_alive='5m'. The run takes 8 minutes. How many times does the 32B model load from disk?",
      "options": [
        {
          "id": "a",
          "text": "3 times — synthesis, and once per wiggum call (wiggum uses a different model but the 32B must reload between calls)",
          "correct": false
        },
        {
          "id": "b",
          "text": "1 time — keep_alive holds it in VRAM for 5 minutes; the synthesis call is the only 32B call",
          "correct": true
        },
        {
          "id": "c",
          "text": "2 times — once at synthesis, once again when wiggum revises (revision uses the producer)",
          "correct": false
        },
        {
          "id": "d",
          "text": "4 times — keep_alive only helps for the exact same model name, and each call reloads",
          "correct": false
        }
      ],
      "explanation": "The 32B producer (pi-qwen-32b) is called only at the synthesis stage. The wiggum evaluator uses Qwen3-Coder:30b — a different model. The planner uses glm4:9b. The revision is done by the producer, but the keep_alive='5m' window covers the 2-minute gap between synthesis and the revision call (wiggum_r2 calls the producer for revision). Total 32B loads: 1 initial + 0 reloads if revision falls within keep_alive window.",
      "hint": "How many of the 4 calls actually use pi-qwen-32b? What does keep_alive do for consecutive same-model calls?"
    },
    {
      "id": "q4_5",
      "type": "multiple-choice",
      "prompt": "A Chrome Trace Event JSON shows two events with `ph: 'X'`, same `ts` timestamp, and `tid: 2` and `tid: 3`. What does this indicate in Perfetto UI?",
      "options": [
        {
          "id": "a",
          "text": "Both events failed — duplicate timestamps indicate an error in the trace",
          "correct": false
        },
        {
          "id": "b",
          "text": "Both events ran in parallel — different thread IDs at the same start time appear in separate Perfetto lanes simultaneously",
          "correct": true
        },
        {
          "id": "c",
          "text": "The events ran sequentially but the logger recorded the same timestamp due to rounding",
          "correct": false
        },
        {
          "id": "d",
          "text": "The events are the same operation logged twice under different thread IDs",
          "correct": false
        }
      ],
      "explanation": "The tid (thread ID) field maps to a separate lane in the Perfetto waterfall. Two events with the same ts and different tids appear in parallel lanes starting at the same time — this is how the panel's 3-persona parallel evaluation is visualized. Same timestamp + different tids = genuine parallelism.",
      "hint": "In Perfetto, each unique tid gets its own horizontal lane. What does same start time + different lanes mean?"
    },
    {
      "id": "q4_6",
      "type": "multiple-choice",
      "prompt": "A model-generated script contains: `__builtins__['exec']('import os; os.system(\"rm -rf /\")')`. Does the AST scanner catch this?",
      "options": [
        {
          "id": "a",
          "text": "Yes — exec is in BLOCKED_BUILTINS and the scanner checks all Call nodes",
          "correct": false
        },
        {
          "id": "b",
          "text": "No — the scanner checks for function calls like exec(), not dictionary subscript access like __builtins__['exec']",
          "correct": true
        },
        {
          "id": "c",
          "text": "Yes — os is in BLOCKED_IMPORTS and the string 'import os' is flagged",
          "correct": false
        },
        {
          "id": "d",
          "text": "Yes — the scanner detects 'rm -rf' as an injection pattern",
          "correct": false
        }
      ],
      "explanation": "The scanner runs two passes. Pass 1 (raw pattern matching) checks for obfuscation patterns: importlib.import_module, __import__(), getattr tricks targeting os/subprocess. __builtins__['exec'](...) uses dictionary subscript access — it matches none of the BLOCKED_ATTR_PATTERNS, so the raw scan passes it through. Pass 2 (AST analysis) checks ast.Call nodes where node.func is an ast.Name (direct function calls like exec()). Subscript access __builtins__['exec'] produces an ast.Subscript node, not ast.Name — the AST walker misses it. Both passes fail: this is a known limitation noted in 'What the Security Layer Cannot Do' — semantic obfuscation that avoids both raw patterns and literal call structure evades the scanner.",
      "hint": "The Security Layer reading explicitly describes what the AST scanner cannot catch."
    },
    {
      "id": "q4_7",
      "type": "multiple-choice",
      "prompt": "The proxy leverage formula is `score × output_lines / runtime_hours`. A harness produces average score 8.4, 130 output lines, in 8 minutes per run. A synthesis instruction change raises the average score to 9.1 but extends runtime to 11 minutes (output lines unchanged). Did leverage improve?",
      "options": [
        {
          "id": "a",
          "text": "Yes — score 9.1 > 8.4 and score is in the numerator, so leverage increased",
          "correct": false
        },
        {
          "id": "b",
          "text": "No — leverage decreased because the 37% runtime increase outweighed the 8% score gain",
          "correct": true
        },
        {
          "id": "c",
          "text": "Yes — the leverage calculation only considers runtime when TAC is provided; without TAC, score alone determines leverage",
          "correct": false
        },
        {
          "id": "d",
          "text": "Cannot be determined — leverage requires the TAC estimate to compute",
          "correct": false
        }
      ],
      "explanation": "Baseline leverage: 8.4 × 130 / (8/60) = 1,092 / 0.133 ≈ 8,190. After change: 9.1 × 130 / (11/60) = 1,183 / 0.183 ≈ 6,453. Leverage fell ~21% even though the score improved ~8%. The runtime increase from 8 → 11 minutes (37.5%) overwhelmed the score gain. This is exactly what the leverage metric is designed to surface: a synthesis instruction change that adds a revision round to reach a higher score is not a net improvement if the latency cost exceeds the quality gain. The proxy formula (score × lines / runtime_hours) is always computable without a TAC estimate — it is the fallback when TAC is not set.",
      "hint": "Calculate leverage for both cases: score × output_lines / (runtime_minutes / 60). Compare the two values."
    }
  ]
}